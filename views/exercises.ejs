<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Ujian Sedang Berlangsung</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
</head>
<body class="bg-light">
  <%- include('partials/_navbar') %>

  <div class="container my-5">
    <div class="row justify-content-center">
      <div class="col-lg-8">
        
        <div class="card border-0 shadow-sm mb-4 bg-primary text-white overflow-hidden">
          <div class="card-body p-4 d-flex justify-content-between align-items-center">
            <div>
              <h4 class="fw-bold mb-1"><i class="bi bi-pen-fill me-2"></i>Mengerjakan Quiz</h4>
              <p class="mb-0 opacity-75">Jawablah dengan teliti dan jujur.</p>
            </div>
            <div class="text-end">
              <span class="display-6 fw-bold"><%= exercises.length %></span>
              <span class="d-block small">Soal</span>
            </div>
          </div>
        </div>

        <form action="/student/<%= userId %>/course/<%= courseId %>/submit" method="POST">
          
          <% exercises.forEach((ex, index) => { %>
            <div class="card border-0 shadow-sm mb-3 hover-effect">
              <div class="card-body p-4">
                <div class="d-flex mb-3">
                  <div class="flex-shrink-0">
                    <span class="badge bg-primary rounded-circle p-3 fs-5" style="width: 50px; height: 50px; display:flex; align-items:center; justify-content:center;">
                      <%= index + 1 %>
                    </span>
                  </div>
                  <div class="flex-grow-1 ms-3">
                    <h5 class="fw-normal lh-base mb-0 pt-2"><%= ex.question %></h5>
                  </div>
                </div>
                
                <div class="mt-3 ps-lg-5 ms-lg-2">
                   <div class="form-floating">
                      <input type="text" class="form-control bg-light border-0" id="ans_<%= ex.id %>" name="answer_<%= ex.id %>" placeholder="Jawaban Anda" autocomplete="off">
                      <label for="ans_<%= ex.id %>" class="text-muted">Ketik jawaban Anda di sini...</label>
                   </div>
                </div>
              </div>
            </div>
          <% }) %>

          <div class="card border-0 shadow-sm mt-4">
            <div class="card-body p-4 text-center">
              <p class="text-muted small mb-3"><i class="bi bi-exclamation-circle"></i> Pastikan semua jawaban sudah terisi sebelum mengirim.</p>
              <button type="submit" class="btn btn-success btn-lg rounded-pill px-5 fw-bold shadow w-100" onclick="return confirm('Yakin ingin mengumpulkan jawaban?')">
                <i class="bi bi-send-fill me-2"></i> Kirim Jawaban
              </button>
            </div>
          </div>

        </form>

      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>