<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Tambah Soal Baru</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
</head>
<body class="bg-light">
  <%- include('partials/_navbar') %>

  <div class="container my-5">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card border-0 shadow-lg">
          <div class="card-header bg-white py-4 text-center border-bottom-0">
            <div class="d-inline-block bg-primary text-white rounded-circle p-3 mb-2 shadow-sm">
               <i class="bi bi-plus-lg display-6"></i>
            </div>
            <h4 class="fw-bold text-dark">Tambah Soal Baru</h4>
            <p class="text-muted small mb-0">Buat pertanyaan yang menantang untuk siswa.</p>
          </div>
          
          <div class="card-body p-4 pt-0">
            <form action="/teacher/<%= teacherId %>/exercise/add" method="POST">
              
              <div class="mb-4">
                <label class="form-label fw-bold text-uppercase small text-muted">Mata Pelajaran / Course</label>
                <div class="input-group">
                  <span class="input-group-text bg-light border-end-0"><i class="bi bi-book"></i></span>
                  <select name="courseId" class="form-select bg-light border-start-0" required>
                    <option value="" disabled selected>-- Pilih Course --</option>
                    <% if (categories.length) { %> 
                      <% categories.forEach(cat => { %>
                        <optgroup label="<%= cat.name %>">
                          <% if (cat.Courses && cat.Courses.length) { %> 
                            <% cat.Courses.forEach(course => { %>
                              <option value="<%= course.id %>"><%= course.name %></option>
                            <% }) %> 
                          <% } else { %>
                            <option disabled> (Kosong) </option>
                          <% } %>
                        </optgroup>
                      <% }) %> 
                    <% } %>
                  </select>
                </div>
              </div>

              <div class="mb-3">
                <label class="form-label fw-bold text-uppercase small text-muted">Pertanyaan</label>
                <div class="form-floating">
                  <textarea name="question" class="form-control bg-light" placeholder="Tulis pertanyaan..." style="height: 100px" required></textarea>
                  <label for="question">Contoh: Apa ibukota Indonesia?</label>
                </div>
              </div>

              <div class="mb-4">
                <label class="form-label fw-bold text-uppercase small text-muted">Kunci Jawaban</label>
                <div class="input-group">
                  <span class="input-group-text bg-success text-white border-0"><i class="bi bi-key-fill"></i></span>
                  <input type="text" name="answerKey" class="form-control bg-light" placeholder="Masukkan jawaban yang benar" required />
                </div>
                <div class="form-text text-muted"><i class="bi bi-info-circle me-1"></i> Jawaban bersifat <em>case-insensitive</em> (huruf besar/kecil tidak berpengaruh).</div>
              </div>

              <hr class="my-4">

              <div class="d-flex justify-content-between">
                <a href="/teacher/<%= teacherId %>" class="btn btn-outline-secondary rounded-pill px-4 fw-bold">
                  <i class="bi bi-arrow-left me-2"></i> Batal
                </a>
                <button type="submit" class="btn btn-primary rounded-pill px-5 fw-bold shadow-sm">
                  Simpan Soal <i class="bi bi-save ms-2"></i>
                </button>
              </div>

            </form>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>