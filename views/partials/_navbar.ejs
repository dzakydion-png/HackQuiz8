<nav class="navbar navbar-expand-lg navbar-dark fixed-top">
  <div class="container">
    <a class="navbar-brand d-flex align-items-center" href="/">
      <i class="bi bi-mortarboard-fill me-2 text-warning"></i> 
      <span>HackQuiz<span class="text-info">8</span></span>
    </a>
    <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto align-items-center">
        <% if (session && session.userId) { %>
          <li class="nav-item">
            <a class="nav-link" href="/<%= session.role %>/<%= session.userId %>">
              <i class="bi bi-grid-fill me-1"></i> Dashboard
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/<%= session.role %>/<%= session.userId %>/profile">
              <i class="bi bi-person-circle me-1"></i> Profil
            </a>
          </li>
          <li class="nav-item ms-lg-3">
            <a href="/logout" class="btn btn-danger btn-sm px-4 rounded-pill shadow-sm">
              <i class="bi bi-box-arrow-right me-1"></i> Logout
            </a>
          </li>
        
        <% } else { %>
          <li class="nav-item">
            <a href="/login" class="nav-link fw-bold">Login</a>
          </li>
          <li class="nav-item ms-lg-2">
            <a href="/register" class="btn btn-light text-primary btn-sm px-4 rounded-pill fw-bold shadow-sm">Register</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>

<div style="height: 80px;"></div>