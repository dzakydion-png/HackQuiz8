<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Student Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
</head>
<body class="bg-light">
  <%- include('partials/_navbar') %>

  <div class="container my-5">
    <div class="text-center mb-5">
      <h2 class="fw-bold">Selamat Datang, Student! 👋</h2>
      <p class="text-muted">Pilih kategori minatmu untuk mulai mengerjakan latihan.</p>
    </div>

    <div class="row g-4 justify-content-center">
      <% if (categories.length > 0) { %>
        <% categories.forEach(c => { %>
        <div class="col-md-4 col-lg-3">
          <div class="card h-100 border-0 shadow-sm text-center p-3 hover-effect">
            <div class="card-body">
              <div class="mb-3">
                 <i class="bi bi-bookmarks-fill display-4 text-primary opacity-50"></i>
              </div>
              <h5 class="card-title fw-bold"><%= c.name %></h5>
              <a href="/student/<%= userId %>/category/<%= c.id %>" class="btn btn-outline-primary rounded-pill mt-3 w-100 stretched-link">
                Lihat Kursus
              </a>
            </div>
          </div>
        </div>
        <% }) %>
      <% } else { %>
        <div class="col-12 text-center">
          <p class="text-muted">Belum ada kategori tersedia saat ini.</p>
        </div>
      <% } %>
    </div>
  </div>
  
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>